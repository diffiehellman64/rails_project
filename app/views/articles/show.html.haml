%h1= @article.title
- if can? :modify, @article
  .actions
    = link_to 'Edit', edit_article_path(@article), class: 'btn btn-default'
    = link_to 'Versions', "/admin/versions/article/#{@article.id}", class: 'btn btn-default'
= @article.created_at.localtime.strftime('%H:%M %d-%m-%Y')
= User.find(@article.user_id).email
= raw @article.text
