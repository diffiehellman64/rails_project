= simple_form_for(@article, remote: true) do |f|
  = f.error_notification
  = f.input :title, autofocus: true
  = f.input :anons, as: :ckeditor, input_html: { ckeditor: { height: 100, toolbar: 'min' } }
  = f.input :text, as: :ckeditor, input_html: { ckeditor: { height: 200 } }
  = f.button :submit
