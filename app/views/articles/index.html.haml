%h1= t('views.articles.index.title')
- if @articles.count > 0
  = will_paginate @articles, renderer: BootstrapPagination::Rails
  - @articles.each do |article|
    %div.list_item
      %span.create_date= article.created_at.localtime.strftime('%Y-%m-%d %H:%M')
      %h3.item_title= link_to article.title, article
      %span.item_prew= raw article.prew
      %span.item_author
        = t('views.articles.author') + ': ' 
        = raw(user_profile_link(article.author))
  = will_paginate @articles, renderer: BootstrapPagination::Rails
- else
  Articles not written yet...
