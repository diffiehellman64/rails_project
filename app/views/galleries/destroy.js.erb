(function() {
  jQuery(function() {
    showAppMessage('<strong>Success!</strong> Gallery deleted!', 'success');
    var galleryId =  '<%= @gallery.id %>';
    $('#data-gallery-id-' + galleryId + ').fadeOut(200)
  });
}).call(this);
