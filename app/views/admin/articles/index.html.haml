%h1 List of articles
- if @articles.count > 0
  %table.table.table-bordered.table-hover
    %tr
      %th Id
      %th Name
      %th Autor
      %th Created
      %th{ colspan: '2' } Actions
    - @articles.each do |article|
      %tr{ 'data-article-id': article.id }
        %td= article.id
        %td= link_to article.title, article
        - autor = User.find(article.user_id)
        %td= autor.email
        %td= article.created_at.localtime.strftime('%H:%M %d-%m-%Y')
        %td
          .btn.btn-default.btn-danger.btn-xs{ id: 'action_article_destroy', 'data-article-id': article.id } Destroy
        %td
          = link_to "Versions (#{article.versions.length})", "#{article.id}/versions", class: 'btn btn-default btn-xs'
- else
  Articles not written yet...
